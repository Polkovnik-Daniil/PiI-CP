весь жизненный цикл mvc =>
жизненный цикл всех моделей(mole view controller) =>
кроме ViewBag => Что есть еще ViewData и Модель представления
Строготипизированне элементы - во вью мы сразу указываем какой тип будет возвращаться на страницу
как обратиться к элементам другого представления из dict в error
как из представления IACTIONRESULT view() => View("Index")
Фильтры => 